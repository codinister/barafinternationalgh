export type PRODUCTS = {
  id: string;
  img: string;
  size: string;
}[];

export type ProductsType = {
  title: string;
  slug: string;
  image: string;
  body: [];
}[];

export type PageType = {
  title: string;
  slug: string;
  image: string;
  excerpt: string;
  body: [];
}[];
